import React from "react";
import { Container } from "./styles";

const Layout: React.FC = () => {
    return (
        <Container>
            <h2>Layout</h2>
        </Container>
    );
};

export default Layout;